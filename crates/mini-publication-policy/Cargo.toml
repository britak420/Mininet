[package]
name = "mini-publication-policy"
version = "0.0.1"
description = "Publication routing plans and checked transport submission with explicit execution limits."
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
publish = false

[dependencies]
# Plans share the existing vocabulary; dispatch uses the existing authenticated
# transport runtime and never invents delivery, payment or storage guarantees.
mini-privacy-policy = { path = "../mini-privacy-policy" }
mini-relay = { path = "../mini-relay" }
mini-resource-pricing = { path = "../mini-resource-pricing" }
mini-transport-policy = { path = "../mini-transport-policy" }
mini-bearer = { path = "../mini-bearer" }
mini-transport-security = { path = "../mini-transport-security" }

[dev-dependencies]
did-mini = { path = "../did-mini" }
mini-crypto = { path = "../mini-crypto" }
